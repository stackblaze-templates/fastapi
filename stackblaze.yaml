services:
- type: web
  name: fastapi-uv
  runtime: python
  repo: https://github.com/stackblaze-examples/fastapi-uv
  plan: starter
  region: ohio
  buildCommand: ./install.sh
  startCommand: ./run.sh
  autoDeployTrigger: commit
version: "1"
